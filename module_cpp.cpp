#include <iostream>
#include <iomanip>
using namespace std;

/*
===============================
 ВАРІАНТ 5 — контрольна з C++
===============================

Три задачі:
1️⃣ Сенсори: підрахунки та середнє
2️⃣ Нормалізація до мінімуму
3️⃣ Команди над масивом

(Кожну можна запускати окремо, просто коментуй інші main-и)
*/

// --------------------------------------------------
// 1️⃣ СЕНСОРИ: підрахунки та середнє
// --------------------------------------------------

/*
Ввід:
7
-2 -1 1 2 3 0 1

Вивід:
pos=3 neg=2 zero=1
even=3 odd=4
avg=2.14
*/

int main1() {
    int n;
    cin >> n;

    int pos = 0, neg = 0, zero = 0, even = 0, odd = 0;
    double sum = 0;
    int valid = 0; // рахуємо тільки числа в межах [-1000;1000]

    for (int i = 0; i < n; i++) {
        int x;
        cin >> x;

        if (x < -1000 || x > 1000) {
            continue; // пропускаємо некоректні
        }

        if (x > 0) {
            pos++;
        } else if (x < 0) {
            neg++;
        } else {
            zero++;
        }

        if (x % 2 == 0) {
            even++;
        } else {
            odd++;
        }

        sum += x;
        valid++;
    }

    double avg = 0;
    if (valid > 0) {
        avg = sum / valid;
    }

    cout << "pos=" << pos << " neg=" << neg << " zero=" << zero << endl;
    cout << "even=" << even << " odd=" << odd << endl;
    cout << fixed << setprecision(2);
    cout << "avg=" << avg << endl;

    return 0;
}


// --------------------------------------------------
// 2️⃣ НОРМАЛІЗАЦІЯ ДО МІНІМУМУ
// --------------------------------------------------

/*
Ввід:
4
3 5 2 8
Вивід:
1 3 0 6
*/

int main2() {
    int n;
    cin >> n;

    int a[100];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    int mn = a[0];
    for (int i = 1; i < n; i++) {
        if (a[i] < mn) {
            mn = a[i];
        }
    }

    for (int i = 0; i < n; i++) {
        cout << a[i] - mn << " ";
    }

    cout << endl;
    return 0;
}


// --------------------------------------------------
// 3️⃣ КОМАНДИ НАД МАСИВОМ
// --------------------------------------------------

/*
Приклади:

Ввід:
5
10 7 12 7 9
S
Вивід:
sum=45

---

Ввід:
5
10 7 12 7 9
A
Вивід:
avg=9.0

---

Ввід:
5
10 7 12 7 9
Z
Вивід:
9 7 12 7 10

---

Ввід:
5
10 7 12 7 9
M
Вивід:
min=7
max=12
*/

int main3() {
    int n;
    cin >> n;

    int a[100];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }

    char cmd;
    cin >> cmd;

    if (cmd == 'S') {
        int sum = 0;
        for (int i = 0; i < n; i++) {
            sum += a[i];
        }
        cout << "sum=" << sum;
    } else if (cmd == 'A') {
        int sum = 0;
        for (int i = 0; i < n; i++) {
            sum += a[i];
        }
        cout << fixed << setprecision(1);
        cout << "avg=" << (double)sum / n;
    } else if (cmd == 'Z') {
        for (int i = n - 1; i >= 0; i--) {
            cout << a[i] << " ";
        }
    } else if (cmd == 'M') {
        int mn = a[0];
        int mx = a[0];
        for (int i = 1; i < n; i++) {
            if (a[i] < mn) {
                mn = a[i];
            }
            if (a[i] > mx) {
                mx = a[i];
            }
        }
        cout << "min=" << mn << endl;
        cout << "max=" << mx;
    }

    cout << endl;
    return 0;
}


// --------------------------------------------------
// ⚙️ ВИБІР ЗАВДАННЯ
// --------------------------------------------------
//
// Для швидкого тесту просто заміни, яку main ти викликаєш:
// main1() → для задачі 1
// main2() → для задачі 2
// main3() → для задачі 3
// --------------------------------------------------

int main() {
    // 🔹 обери, яку функцію хочеш запустити:
    // return main1(); // сенсори
    // return main2(); // нормалізація
    // return main3(); // команди
    return main1();
}

